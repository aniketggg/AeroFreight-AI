[flake8]
# 88 matches black / modern Python style; the default 79 is needlessly strict
# and conflicts with the spec's verbatim Field descriptions.
max-line-length = 100
# E203/W503 conflict with black's slicing + line-break conventions.
extend-ignore = E203, W503
exclude = .venv, __pycache__, .git
